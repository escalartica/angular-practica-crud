export interface Car {
    id: number;       // Identificador único para cada coche
    marca: string;    // Marca del coche
    modelo: string;   // Modelo del coche
    total: number;    // Total (puede ser precio, kilómetros, etc.)
  }
  